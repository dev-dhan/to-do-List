This Simple TO-DO list web app, is mainly create using simple HTML, Vanilla CSS and JS for a little bit adding/deleteing functionality in able for user
to create/add and delete a simple task.

This simple app didn't have any database so when the user refresh the browser the data inputs will be deleted.